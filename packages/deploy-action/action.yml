name: Blode.md Deploy
description: Deploy docs to Blode.md

inputs:
  api-key:
    description: API key
    required: true
  api-url:
    description: API base URL
    required: false
  branch:
    description: Git branch name
    required: false
  commit-message:
    description: Deploy message
    required: false
  directory:
    description: Docs directory
    required: false
  project:
    description: Project slug (reads from docs.json slug if omitted)
    required: false

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Push docs
      shell: bash
      env:
        BLODEMD_API_KEY: ${{ inputs.api-key }}
        BLODEMD_API_URL: ${{ inputs.api-url }}
        BLODEMD_BRANCH: ${{ inputs.branch }}
        BLODEMD_COMMIT_MESSAGE: ${{ inputs.commit-message }}
        BLODEMD_PROJECT: ${{ inputs.project }}
      run: npx --yes blodemd push ${{ inputs.directory }}
