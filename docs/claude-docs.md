I want to create a clone of Mintlify (without the dashboard)

I have 10+ open source projects and the docs are built with fuma docs on NextJS on vercel

I want to create a library called Blode-docs hosted on vercel and a command/github actions/? to push updates.

Each project just needs mdx and a config.

The docs are hosted centrally. No need for a dashboard

Just use vercel for custom domains

Check Blode docs repos for the initial concept but I want it even more headless and instant to setup within a bunch of different repos and GitHub orgs
