# Voters — Blode.md

Distilled from `.claude/knowledge/STRENGTHS-WEAKNESSES.md` and `.claude/knowledge/CAROL.md`. No self-portrait (`asb-who-me`) file exists; candidates come from the product record. Positioning should lead with V1.

## Voters

**V1.** Docs stay in the customer's editor and git. A feature that would pull writing into a CMS, a WYSIWYG, or a plugin marketplace does not ship. [S1, S7]
Extremity: the about page states this as a hard constraint, not a preference — "If a feature does not move docs closer to the code that produced them, it does not ship."
Rare among peers: GitBook's product _is_ the second editor. Mintlify has been adding visual editing. Blode.md's intro post exists to refuse that.
Overpowers: no WYSIWYG, no marketplace, no mixed-team CMS, founder-scale support, pruned `docs.json`. Carol reads those gaps as proof the PR stays the review.
Reverberates: publish paths are git-only; hosted is `$0` because the surface is small; support is the founder; schema is pruned Mintlify, not a plugin platform.
Energy: Matthew Blode wrote the product and the intro post around this refusal.

## Special strengths (near-misses — deploy, do not inflate)

- **MIT, same CLI hosted and self-host.** Extreme for a hosted docs product; not THE-person rare (plenty of MIT static-site generators). Deploy as the self-host plan and the "inspect the renderer" line, not as the H1.
- **Agent Markdown from the same MDX deploy** (`llms.txt`, `llms-full.txt`, per-page `.md`). Real, and Carol's K3 needs it. Not a voter: Mintlify and GitBook also ship llms.txt in 2026. Deploy under the promise (the answer matches the commit), never as the category claim.

## Shaping strengths (true, common — not the bet)

- GitHub App + CLI publish
- Custom domains and `/docs` proxy
- 40 MDX components
- `$0` unlimited seats (a price fact, not an obsession)

## Verdict

One voter survives. The git-native _workflow_ is table stakes against Mintlify; the voter is the **refusal** to add a second editor or a marketplace. Homepage copy must say that refusal out loud (Hidden Multipliers shop pattern: name the anti-reader). Do not pad a second voter.

## Next steps

Lead headlines and the homepage's commiseration beat with V1. Keep `$0` and agent Markdown as supporting facts. Re-run positioning's weakest lines against this file.
