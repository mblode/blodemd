{
	"buildCommand": "npx turbo run build --filter=api...",
	"installCommand": "npm install --prefix=../.. --include-workspace-root",
	"outputDirectory": "dist",
	"functions": {
		"api/dist/index.js": {
			"runtime": "nodejs20.x"
		}
	}
}
