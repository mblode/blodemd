import { SettingsPlaceholder } from "@/components/project-settings/settings-placeholder";

export default function WorkspaceSettingsPage() {
  return (
    <SettingsPlaceholder
      description="Workspace-level settings, members, and billing live here."
      title="Workspace"
    />
  );
}
